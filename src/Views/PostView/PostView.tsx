import React from 'react';

const PostView: React.FunctionComponent<any> = (props) => {

    return (
        <React.Fragment>
            WIP
        </React.Fragment>
    );
};

export default PostView;